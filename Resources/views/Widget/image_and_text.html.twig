<div {% if id is not empty %}id="{{ id }}"{% endif %}class="row {{ html_classes }}">
    <div class="col">
        <img class="{{ image_classes }}" src="{{ image|imagine_filter('generic_thumbnail') }}" alt="{{ image_alt }}" title="{{ image_alt }}">
        {{ text|raw }}
    </div>
</div>
